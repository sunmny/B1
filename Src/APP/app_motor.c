#include "app_motor.h"
/*
//ÉèÖÃ·äÃùÆ÷ÏìÉù´ÎÊı
void APP_set_buzzer_nub(uint8 nub,uint32 ms)
{
	while(nub--)
		{  
  	    Buzzer_Set(1);
        vTaskDelay(ms);
			 //delay_ms(ms);
        Buzzer_Set(0);
			if(nub!=0)
        vTaskDelay(ms);
      // delay_ms(ms);			
    }  

}
*/