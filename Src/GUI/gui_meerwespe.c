//#include "includes.h"
//#include "HW_Key.h"
#include "gui_meerwespe.h"

/*
void GUI_wait_enter(void)
{
  while(1)
	{
    		if(KEY_VALUE_ENTER == key_getkey())
				{
					APP_set_buzzer_nub(1,100);
				break;
				}
        vTaskDelay(10);			
	}

}

void GUI_wait_cancle(void)
{
	
  while(1)
	{
	      
    		if(KEY_VALUE_CANCEL ==key_getkey())
				{
					APP_set_buzzer_nub(1,100);
				break;
				}
        vTaskDelay(10);		    				
						
	}

}

uint8 GUI_wait_enter_cancle(void)
{
	uint8 key=0;
  while(1)
	{
	      key = key_getkey();
    		if(KEY_VALUE_CANCEL ==key)
				{
					APP_set_buzzer_nub(1,100);
				 return 0;
				}else if(KEY_VALUE_ENTER ==key)
				{
				 APP_set_buzzer_nub(1,100);
				 return 1;
				}
        vTaskDelay(10);		    				
						
	}

}
*/
